-- ===== Services =====
local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local player = Players.LocalPlayer

-- ===== UI =====
local screenGui = Instance.new("ScreenGui", player:WaitForChild("PlayerGui"))
screenGui.Name = "WebSlingerUI"
screenGui.ResetOnSpawn = false

local mainFrame = Instance.new("Frame", screenGui)
mainFrame.Size = UDim2.new(0, 200, 0, 260)  -- ปรับให้เท่ากับ Laser
mainFrame.Position = UDim2.new(1, -280, 0, 60)
mainFrame.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
mainFrame.BorderSizePixel = 0
mainFrame.Active = true
mainFrame.ClipsDescendants = true


-- มุมโค้งมน
local corner = Instance.new("UICorner", mainFrame)
corner.CornerRadius = UDim.new(0, 12)

-- เส้นขอบสายรุ้ง
local stroke = Instance.new("UIStroke", mainFrame)
stroke.Thickness = 3
stroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
stroke.Color = Color3.fromRGB(255, 0, 0)

-- ===== ปุ่ม Auto Web Slinger =====
local autoLaserBtn = Instance.new("TextButton", mainFrame)
autoLaserBtn.Size = UDim2.new(1, -20, 0, 40)
autoLaserBtn.Position = UDim2.new(0, 10, 0, 10)
autoLaserBtn.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
autoLaserBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
autoLaserBtn.Font = Enum.Font.GothamBold
autoLaserBtn.TextSize = 16
autoLaserBtn.Text = "Auto Web Slinger OFF"
autoLaserBtn.AutoButtonColor = false
local btnCorner = Instance.new("UICorner", autoLaserBtn)
btnCorner.CornerRadius = UDim.new(0, 8)

-- ===== Label รายชื่อ Pull =====
local playerListLabel = Instance.new("TextLabel", mainFrame)
playerListLabel.Size = UDim2.new(1, -20, 0, 20)
playerListLabel.Position = UDim2.new(0, 10, 0, 60)
playerListLabel.BackgroundTransparency = 1
playerListLabel.Text = "เลือกผู้เล่นที่จะดึง (<=20 stud):"
playerListLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
playerListLabel.Font = Enum.Font.Gotham
playerListLabel.TextSize = 14
playerListLabel.TextXAlignment = Enum.TextXAlignment.Left

-- ===== Scroll รายชื่อ =====
local playerListFrame = Instance.new("ScrollingFrame", mainFrame)
playerListFrame.Size = UDim2.new(1, -20, 0, 140) -- ปรับให้เท่ากับ Laser
playerListFrame.Position = UDim2.new(0, 10, 0, 85)
playerListFrame.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
playerListFrame.BorderSizePixel = 0
playerListFrame.ScrollBarThickness = 6
local listCorner = Instance.new("UICorner", playerListFrame)
listCorner.CornerRadius = UDim.new(0, 8)

local listLayout = Instance.new("UIListLayout", playerListFrame)
listLayout.Padding = UDim.new(0, 4)
listLayout.SortOrder = Enum.SortOrder.LayoutOrder

-- ===== Drag Support =====
local dragging, dragInput, dragStart, startPos
local function update(input)
    local delta = input.Position - dragStart
    mainFrame.Position = UDim2.new(
        startPos.X.Scale, startPos.X.Offset + delta.X,
        startPos.Y.Scale, startPos.Y.Offset + delta.Y
    )
end
mainFrame.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = true
        dragStart = input.Position
        startPos = mainFrame.Position
        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then dragging = false end
        end)
    end
end)
mainFrame.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then dragInput = input end
end)
UserInputService.InputChanged:Connect(function(input)
    if input == dragInput and dragging then update(input) end
end)

-- ===== Rainbow Border =====
task.spawn(function()
    local t = 0
    while screenGui.Parent do
        t = t + RunService.Heartbeat:Wait()
        local r = math.sin(t*2) * 127 + 128
        local g = math.sin(t*2 + 2) * 127 + 128
        local b = math.sin(t*2 + 4) * 127 + 128
        stroke.Color = Color3.fromRGB(r, g, b)
    end
end)

-- ===== Auto Web Slinger Logic =====
local AutoLaser = false
local LaserDistance = 20
local Net = ReplicatedStorage:WaitForChild("Packages"):WaitForChild("Net")
local Remote = Net:WaitForChild("RE/UseItem")

local function getRoot()
    local char = player.Character
    if char then return char:FindFirstChild("HumanoidRootPart") end
end

local function equipWebSlinger()
    local backpack = player:WaitForChild("Backpack")
    local character = player.Character
    local humanoid = character:FindFirstChild("Humanoid")
    local tool = backpack:FindFirstChild("Web Slinger") or character:FindFirstChild("Web Slinger")
    if tool and humanoid then
        if tool.Parent ~= character then
            humanoid:EquipTool(tool)
            RunService.RenderStepped:Wait()
        end
        return tool
    end
    return nil
end

local function unequipWebSlinger()
    local character = player.Character
    local backpack = player:WaitForChild("Backpack")
    local tool = character:FindFirstChild("Web Slinger")
    if tool then tool.Parent = backpack end
end

-- ===== Pull Logic =====
local PullDistance = 20
local PullPosition = Vector3.new(-407.225, -6.789, 77.851)

local function pullPlayer(targetPlayer)
    if targetPlayer and targetPlayer.Character then
        local myRoot = player.Character and player.Character:FindFirstChild("HumanoidRootPart")
        local targetRoot = targetPlayer.Character:FindFirstChild("HumanoidRootPart")
        if myRoot and targetRoot then
            local distance = (myRoot.Position - targetRoot.Position).Magnitude
            if distance <= PullDistance then
                local args = {Vector3.new(PullPosition.X, PullPosition.Y, PullPosition.Z), targetRoot}
                Remote:FireServer(unpack(args))
            else
                print("ผู้เล่น "..targetPlayer.Name.." อยู่เกินระยะ "..PullDistance.." stud, ไม่ดึง")
            end
        end
    end
end

local currentSelectedButton = nil

local function createPlayerButtons()
    playerListFrame:ClearAllChildren()
    local listLayout = Instance.new("UIListLayout", playerListFrame)
    listLayout.Padding = UDim.new(0, 4)
    listLayout.SortOrder = Enum.SortOrder.LayoutOrder

    for _, plr in pairs(Players:GetPlayers()) do
        if plr ~= player then
            local btn = Instance.new("TextButton", playerListFrame)
            btn.Size = UDim2.new(1, -10, 0, 25)
            btn.BackgroundColor3 = Color3.fromRGB(60, 60, 60)
            btn.TextColor3 = Color3.fromRGB(255, 255, 255)
            btn.Text = " " .. plr.DisplayName
            btn.Font = Enum.Font.Gotham
            btn.TextSize = 14
            btn.AutoButtonColor = false

            local c = Instance.new("UICorner", btn)
            c.CornerRadius = UDim.new(0, 6)

            btn.MouseButton1Click:Connect(function()
                -- เปลี่ยนสีปุ่มเก่าเป็นปกติ
                if currentSelectedButton then
                    currentSelectedButton.BackgroundColor3 = Color3.fromRGB(60,60,60)
                end

                -- เปลี่ยนสีปุ่มที่เลือกเป็นสีแดง
                btn.BackgroundColor3 = Color3.fromRGB(200,50,50)
                currentSelectedButton = btn

                -- ยิง Laser ไปยังผู้เล่นที่เลือก
                pullPlayer(plr)
            end)
        end
    end
    playerListFrame.CanvasSize = UDim2.new(0, 0, 0, #Players:GetPlayers()*30)
end

local currentSelectedButton = nil
local currentSelectedPlayer = nil

local function createPlayerButtons()
    playerListFrame:ClearAllChildren()

    -- สร้าง Layout หนึ่งครั้งเท่านั้น
    local listLayout = Instance.new("UIListLayout")
    listLayout.Padding = UDim.new(0,4)
    listLayout.SortOrder = Enum.SortOrder.LayoutOrder
    listLayout.Parent = playerListFrame

    for _, plr in pairs(Players:GetPlayers()) do
        if plr ~= player then
            local btn = Instance.new("TextButton", playerListFrame)
            btn.Size = UDim2.new(1, -10, 0, 25)
            btn.BackgroundColor3 = Color3.fromRGB(60,60,60)
            btn.TextColor3 = Color3.fromRGB(255,255,255)
            btn.Text = " " .. plr.DisplayName
            btn.Font = Enum.Font.Gotham
            btn.TextSize = 14
            btn.AutoButtonColor = false

            local c = Instance.new("UICorner", btn)
            c.CornerRadius = UDim.new(0,6)

            btn.MouseButton1Click:Connect(function()
                -- รีเซ็ตสีปุ่มเดิม
                if currentSelectedButton then
                    currentSelectedButton.BackgroundColor3 = Color3.fromRGB(60,60,60)
                end
                -- ตั้งปุ่มปัจจุบันเป็นสีแดง
                btn.BackgroundColor3 = Color3.fromRGB(200,50,50)
                currentSelectedButton = btn
                currentSelectedPlayer = plr
            end)
        end
    end

    -- ปรับ CanvasSize ให้รองรับทุกคน
    local totalPlayers = #Players:GetPlayers() - 1
    playerListFrame.CanvasSize = UDim2.new(0,0,0,totalPlayers*30)
end


-- ===== สร้างปุ่ม Refresh =====
local refreshBtn
local function createRefreshBtn()
    refreshBtn = Instance.new("TextButton", mainFrame)
    refreshBtn.Size = UDim2.new(1, -20, 0, 30)
    refreshBtn.Position = UDim2.new(0, 10, 1, -40)
    refreshBtn.BackgroundColor3 = Color3.fromRGB(70, 70, 70)
    refreshBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
    refreshBtn.Font = Enum.Font.GothamBold
    refreshBtn.TextSize = 14
    refreshBtn.Text = "🔄 Refresh Players"
    local refreshCorner = Instance.new("UICorner", refreshBtn)
    refreshCorner.CornerRadius = UDim.new(0, 6)

    refreshBtn.MouseButton1Click:Connect(function()
        createPlayerButtons()
    end)
end

-- ===== ปุ่มย่อ/ขยาย UI =====
local uiMinimized = false
local minimizeBtn = Instance.new("TextButton", mainFrame)
minimizeBtn.Size = UDim2.new(0, 20, 0, 20)
minimizeBtn.Position = UDim2.new(1, -30, 0, 10)
minimizeBtn.Text = "–"
minimizeBtn.Font = Enum.Font.GothamBold
minimizeBtn.TextSize = 14
minimizeBtn.BackgroundColor3 = Color3.fromRGB(60,60,60)
minimizeBtn.TextColor3 = Color3.fromRGB(255,255,255)
local minCorner = Instance.new("UICorner", minimizeBtn)
minCorner.CornerRadius = UDim.new(0,4)

minimizeBtn.MouseButton1Click:Connect(function()
    uiMinimized = not uiMinimized
    if uiMinimized then
        mainFrame.Size = UDim2.new(0, 200, 0, 60)
        playerListFrame.Visible = false
        playerListLabel.Visible = false
        if refreshBtn then refreshBtn:Destroy() end
        minimizeBtn.Text = "+"
    else
        mainFrame.Size = UDim2.new(0, 200, 0, 320)
        playerListFrame.Visible = true
        playerListLabel.Visible = true
        createRefreshBtn()
        minimizeBtn.Text = "–"
    end
end)

-- ===== ปุ่ม Auto Web Slinger =====
autoLaserBtn.MouseButton1Click:Connect(function()
    AutoLaser = not AutoLaser
    autoLaserBtn.Text = "Auto Web Slinger " .. (AutoLaser and "ON" or "OFF")
    autoLaserBtn.BackgroundColor3 = AutoLaser and Color3.fromRGB(50,200,100) or Color3.fromRGB(50,50,50)
    if not AutoLaser then unequipWebSlinger() end
end)

-- ปิดอัตโนมัติถ้า UI ถูกลบ
screenGui.AncestryChanged:Connect(function(_, parent)
    if not parent and AutoLaser then
        AutoLaser = false
        autoLaserBtn.Text = "Auto Web Slinger OFF"
        autoLaserBtn.BackgroundColor3 = Color3.fromRGB(50,50,50)
        unequipWebSlinger()
    end
end)

-- ===== Main loop Auto Web Slinger =====
spawn(function()
    while true do
        if AutoLaser then
            local root = getRoot()
            if root then
                local myPos = root.Position
                local tool = equipWebSlinger()
                if tool then
                    local targetPlayer = currentSelectedPlayer -- ใช้คนที่เลือกจาก UI
                    if targetPlayer and targetPlayer.Character and targetPlayer.Character:FindFirstChild("UpperTorso") then
                        local targetPart = targetPlayer.Character.UpperTorso
                        local dist = (myPos - targetPart.Position).Magnitude
                        if dist <= LaserDistance then
                            tool:Activate()
                            Remote:FireServer(targetPart.Position, targetPart)
                        else
                            print("ผู้เล่น "..targetPlayer.Name.." อยู่เกินระยะ "..LaserDistance.." stud, ไม่ยิง")
                        end
                    end
                end
            end
        end
        task.wait(0.01)
    end
end)


-- ===== โหลดครั้งแรก =====
createPlayerButtons()
createRefreshBtn()
